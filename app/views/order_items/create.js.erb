<% if @order.errors.full_messages.any? || @order_item.errors.full_messages.any? %>
alert("NOT VALID.")
<% else %>
  $(".cart-text").html("<%= escape_javascript(render 'layouts/cart_text') %>")
<% end %>
